# Правки Levmich Studio за 29 мая 2026

Папка сайта: `D:/Main_projects/levmich_studio_site_drafts/levmich-studio-site`

## Изменённые файлы

- `index.html`
- `info.html`
- `case-front.html`
- `case-three.html`
- `case-wedding.html`
- `Assets/styles/style.css`
- `Assets/scripts/main-v2.js`
- `CHANGES_TODAY.md`
- Изображения: 25 PNG → WebP (см. ниже)

## Hero

- Удалена рудиментарная инфо-плашка `.hero__info` («Новый проект / …») из всех трёх карточек — и в десктоп-, и в мобильной версии.
- В мобильную версию добавлена единая плашка-подсказка «Подробнее» (`.hero__pill`, белая пилюля с иконкой курсора, как у десктопного `.case-cursor`).
  - Зафиксирована по центру вверху большой карточки — НЕ едет вместе с фото.
  - Плавно появляется сверху (slide-down + fade) ПОСЛЕ того, как изображение увеличилось.
  - Плавно исчезает вверх ПЕРЕД тем, как изображение уменьшается (старт ротации сдвинут, чтобы плашка успела уйти).
  - Текст плашки локализуется (RU «Подробнее» / EN «Details»).
- На десктопе плашек у Hero больше нет — остаётся только курсор-подсказка «Подробнее» при наведении.

## Анимации на iPhone (iOS Safari)

- Причина «не работают анимации»: многие iPhone сообщают `prefers-reduced-motion: reduce` (Accessibility / Low Power Mode), а код глушил по этому флагу почти всю анимацию (reveal при скролле, stacked-scroll портфолио/услуг, intro Hero, параллакс, Lenis).
- Reveal при скролле переписан с ScrollTrigger на `IntersectionObserver` (надёжно работает в iOS Safari) и больше не зависит от reduce-motion.
- Снят гейт reduce-motion со stacked-scroll кейсов/услуг и с intro-анимации Hero.
- Добавлен `ScrollTrigger.refresh()` по `load` и `document.fonts.ready` — iOS мис-меряет позиции из-за динамической адресной строки и поздней загрузки шрифтов.
- Декоративные эффекты, привязанные к курсору (параллакс за мышью, edge-reflection, курсор-подсказка) и параллакс фото в About оставлены под reduce-motion — это собственно «вестибулярная» анимация, на тач-устройствах курсорные эффекты и так не применяются.

## Изображения (ускорение загрузки)

- Все используемые растровые изображения сконвертированы из PNG в WebP, качество q=90 (визуально без потерь).
- Суммарно по используемым картинкам: **50.1 МБ → 3.9 МБ** (≈ 8% от исходного, экономия ~46 МБ).
- Ссылки в HTML переведены `.png` → `.webp` (index + 3 страницы кейсов, включая мобильные варианты в `<picture>`).
- Исходные 25 PNG удалены из репозитория (история git их сохраняет).

## Cache

- CSS обновлён до `style.css?v=105` на всех страницах.
- JS обновлён до `main-v2.js?v=69` на всех страницах (раньше index=68, остальные=64).

## Что проверить перед/после публикации

- iPhone (главная и страницы кейсов): при скролле блоки должны появляться, портфолио/услуги — стекаться, Hero — листаться. Если на устройстве включено «Уменьшение движения» — анимации теперь всё равно идут.
- Мобильный Hero: плашка «Подробнее» по центру вверху карточки появляется после увеличения и исчезает перед уменьшением, на каждой карточке, в одном месте.
- Десктоп Hero: плашек нет, при наведении — курсор-подсказка «Подробнее».
- Все изображения грузятся (WebP) и выглядят без артефактов.
